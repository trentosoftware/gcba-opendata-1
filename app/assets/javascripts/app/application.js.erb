$(document).ready(function(){

    _.templateSettings = {
        interpolate: /\{\{\=(.+?)\}\}/g,
        evaluate: /\{\{(.+?)\}\}/g
    };

    StatusBar.init();
    Map.init();
    Searchbox.init(Map);

    $('#introduction-modal').modal('show');
});